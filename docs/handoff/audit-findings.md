# Audit findings (2026-09-12)

Generated from the 53-agent bug audit (10 area finders, 43 skeptical verifiers). Every row was confirmed by a verifier that tried to refute it; severities are the verifier's. Status: `fixed` = addressed in the working tree on 2026-09-13; `agent redesign` = belongs to the agent-identification pipeline and is deferred to that redesign (see `agent-redesign-handoff.md`); `open` = still to do. Classification `unclear` = a product decision for the maintainer.

## High (6)

- **F079** `.github/workflows/release.yml:13` — high · bug · _fixed 2026-09-13_  
  release.yml pins LAZYDIFF_REF v0.1.0-alpha.18 on Xubbbb/lazydiff, which carries no tags: every release build leg fails at 'Checkout lazydiff', so the fork can never publish a release and fresh TPM installs of the fork 404 on binary download
- **F013** `apps/server-rs/src/lib.rs:569` — high · bug · _agent redesign_  
  Last-focused pane of a background session is treated as still on screen, so agents finishing there are marked seen immediately and never show the unseen marker  
  Fix: Only treat a pane as seen while a client is actually looking at it. Minimal version consistent with the existing single-client focus tracking: in `remember_focused_pane` and `sync_agent_pane_presence`, drop entries for every session other than the one the client just reported (`focused_pane_by_session.retain(|s, _| s == &context.session)` before the insert), so `mark_focused_agent_panes_seen`, `apply_agent_event` and `apply_agent_watcher_snapshot` only see the current session's pane. Cleaner ver
- **F080** `opensessions.tmux:79` — high · bug · _fixed 2026-09-13_  
  opensessions-server outlives its tmux server and is silently reused after a tmux restart, so the new tmux server never gets any opensessions hooks
- **F021** `packages/runtime-rs/src/tmux_provider.rs:160` — high · bug · _fixed 2026-09-13_  
  list-panes -s -t <session_name> / display-message -t <session_name> resolve a bare name as a window of the *current* session first, so per-session pane queries return another session's panes
- **F022** `packages/runtime-rs/src/tmux_provider.rs:566` — high · bug · _fixed 2026-09-13_  
  after-resize-pane width-repair hook re-triggers itself indefinitely whenever the sidebar cannot reach @opensessions_width (stacked/zoomed layouts) - sustained fork storm on the tmux server
- **F036** `packages/runtime-rs/src/tracker.rs:568` — high · bug · _agent redesign_  
  Single-entry pane attachment re-stamps the entry on every pane of that agent (last listed pane wins) and is never re-evaluated when a second entry appears, so kill/focus/seen route to the wrong thread and a restarted conversation in the same pane is never attached  
  Fix: In the thread-id-less fallback (tracker.rs:568-595): count the panes of that agent in this presence batch and only auto-attach when there is exactly one pane and exactly one candidate entry (otherwise leave pane_id untouched); never restamp an entry that is already Alive on another pane still present in the batch. When several entries exist for an agent with a single pane, attach the pane to the most recent non-terminal entry (max ts, preferring Running/ToolRunning/Waiting) and clear pane_id/liv

## Medium (41)

- **F090** `.github/workflows/auto-version.yml:68` — medium · bug · _fixed 2026-09-13_  
  auto-version has no concurrency control and pushes non-atomically: a second push to main during a run leaves an orphan version tag that either skips a release or permanently wedges auto-versioning (every later run green but no-op)
- **F085** `.github/workflows/release.yml:127` — medium · bug · _fixed 2026-09-13_  
  release.yml has no ref guard for workflow_dispatch: running it from a branch (the UI/CLI default is main) publishes a release and creates a tag named 'main', after which auto-version's `git push origin main --tags` fails with 'src refspec main matches more than one' on every push
- **F014** `apps/server-rs/src/lib.rs:315` — medium · bug · _fixed 2026-09-13_  
  Background `git status --porcelain` every 5 s takes `.git/index.lock` on every run, so concurrent user/agent git commands can fail with 'index.lock: File exists'
- **F107** `apps/server-rs/src/lib.rs:382` — medium · bug · _fixed 2026-09-13_  
  config.json `theme`, `sidebarPosition`, `sessionFilter` are parsed but never applied, and `set-theme`/`set-filter` are never persisted (regression from the TS server); theme resets to the default on every server restart
- **F094** `apps/server-rs/src/lib.rs:414` — medium · bug · _fixed 2026-09-13_  
  Session order and hidden set are never persisted: server always builds SessionOrder::new(None), so ~/.config/opensessions/session-order.json is never written or read despite four doc promises
- **F002** `apps/server-rs/src/lib.rs:636` — medium · bug · _fixed 2026-09-13_  
  cleanup_mux_hooks kills the sidebar panes before cleanup_hooks scans for them, so remain-on-exit is never reset on sidebar windows after /quit or SIGTERM (regression in the uncommitted change)
- **F005** `apps/server-rs/src/lib.rs:2207` — medium · bug · _fixed 2026-09-13_  
  run_process_with_timeout never drains the child's pipes, so any sqlite3 result larger than the pipe buffer (64 KiB on Linux) deadlocks until the 500 ms kill and OpenCode detection silently stops
- **F003** `apps/server-rs/src/lib.rs:2492` — medium · bug · _fixed 2026-09-13_  
  The opensessions server outlives a tmux server restart and never re-installs its hooks; the plugin reuses the stale hook-less server for the new tmux server
- **F001** `apps/server-rs/src/lib.rs:3076` — medium · bug · _fixed 2026-09-13_  
  Untrusted Content-Length is pre-allocated and zero-filled before the body is read: an oversized value aborts the whole server (and, in the shipped panic=abort release build, a value near usize::MAX aborts it too via wrap + slice panic)
- **F070** `apps/tui-rs/src/main.rs:210` — medium · bug · _open_  
  Freshly spawned sidebar pane is the active pane for ~50-250 ms; keystrokes typed in that window are swallowed (before first State) or executed by the sidebar (after it, until the TUI's own refocus) - a stray 'q' quits the whole server  
  Fix: Spawn the sidebar with split-window -d (tmux_provider.rs:283: vec!["split-window", side, "-d", "-f", "-l", &size]) so the user's pane never loses focus; then either delete do_startup_refocus or make it select the pane that was active before the split (the server already receives it in the /ensure-sidebar context) instead of the first non-sidebar pane. Gating handle_key until startup_refocused would only stop the executed-key case, not the swallowed keystrokes, so -d is the real fix.
- **F071** `apps/tui-rs/src/runtime_context.rs:94` — medium · bug · _fixed 2026-09-13_  
  Startup refocus selects the first non-sidebar pane of the window instead of the previously active pane, so every sidebar spawn moves focus in multi-pane windows (current and background) and the resulting after-select-pane /focus hooks record sidebar-driven selections as user focus
- **F084** `docs/reference/configuration.md:118` — medium · bug · _fixed 2026-09-13_  
  Docs tell local-checkout users to `source-file opensessions.tmux`; tmux parses the bash script as config, errors at line 22, registers no keybindings, and leaks a bogus CURRENT_DIR into every new pane
- **F108** `docs/reference/programmatic-api.md:7` — medium · bug · _fixed 2026-09-13_  
  Programmatic API docs (and README/CONTRACTS examples) hard-code port 7391, which a plugin-launched server never uses (it listens on 22000+hash(tmux socket)); no discovery path is documented or exported to panes
- **F082** `integrations/tmux-plugin/scripts/focus.sh:24` — medium · bug · _fixed 2026-09-13_  
  focus.sh 'reveal and focus' POSTs /toggle when the current window has no sidebar, which hides every other window's sidebar instead of revealing one here
- **F087** `integrations/tmux-plugin/scripts/install-binaries.sh:38` — medium · bug · _fixed 2026-09-13_  
  install-binaries.sh downloads with no curl/wget timeout and opensessions.tmux runs it synchronously during config load, so a stalled GitHub/proxy connection hangs the first tmux client (and any retry) indefinitely
- **F081** `integrations/tmux-plugin/scripts/server-common.sh:72` — medium · bug · _fixed 2026-09-13_  
  Loopback curl calls in helper scripts and generated tmux hooks honour http_proxy; with a down or error-page proxy server_alive misreports and the plugin stops working
- **F109** `integrations/tmux-plugin/scripts/server-common.sh:110` — medium · bug · _fixed 2026-09-13_  
  opensessions server outlives `tmux kill-server`; a restarted tmux on the same socket reuses it via server_alive but hooks are never reinstalled
- **F083** `integrations/tmux-plugin/scripts/uninstall.sh:66` — medium · bug · _fixed 2026-09-13_  
  remain-on-exit is never switched back off for windows that hosted a sidebar: both uninstall.sh and the server's cleanup look for sidebar panes only after those panes were killed
- **F086** `opensessions.tmux:81` — medium · bug · _fixed 2026-09-13_  
  Update-restart in opensessions.tmux blindly SIGTERMs whatever pid is in the pid file: a stale pid leaves the old server running (README promise broken) and a recycled pid kills an unrelated process
- **F039** `packages/runtime-rs/src/agent_watchers.rs:416` — medium · bug · _agent redesign_  
  Claude Code project-dir decoding trusts the dash->slash guess whenever that path exists and the encoded fallback is exact-match only, so transcripts are misattributed on name collisions and never resolved for subdirectory launches under dashed/underscored/dotted session dirs; the per-entry cwd is ignored  
  Fix: In claude_code_snapshot_from_jsonl take project_dir from the first entry with a non-empty string `cwd` (every user/assistant entry has one) and use the decoded folder name only as a fallback. In the fallback path, check for an exact encoded match against known session dirs before trusting the naive slash path, and apply parent/child matching on the encoded form (e.g. `encoded.starts_with(&format!("{}-", encode(session.dir)))`), routing `__encoded__` values through resolve_session_for_project_dir
- **F041** `packages/runtime-rs/src/agent_watchers.rs:521` — medium · bug · _agent redesign_  
  Codex parser turns injected user items (<recommended_plugins>, <skill>, '# Context from my IDE setup:' blobs, guardian-subagent assessment prompts) into thread titles and last prompts; the event_msg '<' filter is bypassed by the prompt fallback  
  Fix: Do the rejection in one place: in normalize_codex_user_prompt, locate the *last* `## My request for Codex:` marker with rfind and keep only the text after it; then reject candidates that start with '<' or '{' or '# Context from my IDE' or '# Files mentioned by the user:' or '# AGENTS.md' (this also fixes the fallback at lines 144-147 and makes the event_msg check redundant). Separately skip rollouts whose first session_meta has `payload.source.subagent` or `payload.thread_source` in {"subagent",
- **F017** `packages/runtime-rs/src/agent_watchers.rs:753` — medium · bug · _fixed 2026-09-13_  
  find_uuid_suffix slices by byte offset: a non-ASCII .jsonl name (>= 36 bytes) under ~/.codex/sessions panics the scan; release profile (panic=abort) turns it into a server abort on the first 2 s tick
- **F016** `packages/runtime-rs/src/project_dir_session.rs:35` — medium · bug · _agent redesign_  
  A session at an ancestor directory (e.g. $HOME) makes an agent under a deeper session's dir unresolvable: nested ancestor matches are treated as ambiguous instead of deepest-wins  
  Fix: In resolve_session_for_project_dir, rank ancestors by depth so the longest prefix wins instead of pooling all ancestors: partition related dirs into ancestors (project_dir.starts_with("{dir}/")) and descendants (dir.starts_with("{project_dir}/")); keep only the deepest ancestor dir's sessions (e.g. `let mut deepest: Option<(&String,&Vec<String>)>; if deepest.is_none_or(|(d,_)| dir.len() > d.len()) { deepest = Some((dir, sessions)) }`), then related_matches = descendants ∪ deepest-ancestor sessio
- **F103** `packages/runtime-rs/src/session_order.rs:45` — medium · bug · _open_  
  Reordering one session while a filter or a collapsed worktree group hides others rewrites the whole order: hidden sessions and collapsed-group members are pushed to the bottom (collapsed group header visibly jumps to the end)  
  Fix: Minimal fix in `SessionOrder::set_visible_order` (session_order.rs:45-56): merge instead of prefix-replace - walk the existing `self.order`, and for each slot whose name is in the visible set emit the next name from the new visible sequence, otherwise keep the (non-visible) name in place; append any leftover visible names at the end. Example: old [a,b,c], visible [c,a] -> [c,b,a]; old [a,g1,g2,b,c], visible [a,c,b] -> [a,g1,g2,c,b]; when everything is visible the result equals the client's list,
- **F025** `packages/runtime-rs/src/tmux_provider.rs:573` — medium · bug · _fixed 2026-09-13_  
  Server shutdown (and toggle-hide) leaves `remain-on-exit on` on every window that hosted a sidebar because the reset scans for sidebar panes after they were killed
- **F027** `packages/runtime-rs/src/tmux_provider.rs:647` — medium · bug · _fixed 2026-09-13_  
  Once a client is in `_os_stash`, every window/session switch spawns another sidebar there and nothing (toggle-off, orphan sweep, shutdown cleanup) ever removes them
- **F028** `packages/runtime-rs/src/tmux_provider.rs:919` — medium · bug · _fixed 2026-09-13_  
  agent_from_pane substring matching with short aliases and fixed priority misattributes panes: tracked agents lose their pane id (title 'sample'/'example' -> amp) or get bound to an unrelated shell pane; it does NOT surface phantom agents in the sidebar
- **F004** `packages/runtime-rs/src/tmux_scripting.rs:63` — medium · bug · _fixed 2026-09-13_  
  Hook and plugin curl commands honour http_proxy (no --noproxy), so a dead or remote proxy in the tmux server environment silently disables every server hook and fools server_alive()
- **F024** `packages/runtime-rs/src/tmux_scripting.rs:189` — medium · bug · _fixed 2026-09-13_  
  Session names are expanded unescaped inside the single-quoted `-d '...'` hook body: an apostrophe silently disables the focus/ensure-sidebar hooks for that session and `'; cmd; '` executes cmd in tmux's run-shell
- **F023** `packages/runtime-rs/src/tmux_scripting.rs:268` — medium · bug · _fixed 2026-09-13_  
  pane-died hook's final sweep kills every dead pane on the tmux server, including panes in windows/sessions where the user (not opensessions) enabled remain-on-exit
- **F038** `packages/runtime-rs/src/tracker.rs:204` — medium · bug · _agent redesign_  
  Waiting and Idle entries are excluded from both prune rules, so an HTTP-posted waiting/idle agent whose pane closes (e.g. Amp's session-start 'idle') is shown forever  
  Fix: In prune_terminal (or a new prune_exited rule run from prune_agents) apply the exited_at branch to every non-Alive entry regardless of status before the is_terminal_status check, so any entry whose pane has been gone for EXITED_PRUNE_MS is reaped; and extend prune_stuck's status match to include Waiting and Idle so silent pane-less waiting/idle entries fall under the 30-minute STUCK_PRUNE_MS TTL.
- **F035** `packages/runtime-rs/src/tracker.rs:443` — medium · bug · _agent redesign_  
  Presence sync equates 'pane not detected as an agent pane' with 'pane gone': a documented custom-integration paneId is erased in the response to the event itself, and (new in this pass) its terminal entries are reaped ~10 s later while the pane is alive  
  Fix: Give the tracker the full set of live pane ids of the session, not just detected agent panes: add a provider method (e.g. list_pane_ids(session) built from list_panes(PaneScope::Session)) and pass it as a second argument to apply_pane_presence; use that set (unioned with the agent-pane ids) for the Exited check at tracker.rs:454-458 while keeping the agent-pane list for attachment. Treat a failed/empty tmux listing (non-zero exit_code in run()) as 'unknown' and skip the downgrade for that sync.
- **F110** `packages/runtime-rs/src/tracker.rs:454` — medium · bug · _agent redesign_  
  Custom (non-alias) HTTP agents lose paneId on the first snapshot and their done/error markers are reaped after ~10 s because pane presence only recognises AGENT_ALIASES panes  
  Fix: Make liveness depend on whether the tracked pane still exists rather than on alias recognition: in sync_agent_pane_presence also collect the session's full live pane-id set (provider.list_panes / a small new MuxProvider method) and pass it to apply_pane_presence, which should only flip an Alive entry to Exited when event.pane_id is absent from that full set (alias-recognised panes keep driving attachment/re-attachment). Alternatively have list_agent_panes include any pane whose id is in tracker.
- **F037** `packages/runtime-rs/src/tracker.rs:703` — medium · bug · _agent redesign_  
  Every applied event unconditionally clears exited_at, so watcher-derived same-ts Waiting/Stale flips and the /exit Done cancel the documented 10-second reap; killed or exited Claude Code entries linger up to 30 minutes  
  Fix: At tracker.rs:700-703 only clear exited_at when the event demonstrates the agent is back: it carries pane_id/liveness Alive, or its status is non-terminal and its ts is strictly newer than the previously stored event's ts. Keep exited_at across terminal events and across watcher transitions whose ts did not advance. Consider also letting prune_terminal reap non-terminal entries via the exited_at rule (see F038) so a killed Running agent does not wait for the Stale flip.
- **F050** `packages/sidebar-core-rs/src/app.rs:257` — medium · bug · _fixed 2026-09-13_  
  Theme picker: a State broadcast reverts the live preview, so Enter re-sends the old theme
- **F048** `packages/sidebar-core-rs/src/app.rs:273` — medium · bug · _fixed 2026-09-13_  
  Every State broadcast resets a wheel-scrolled session list back to the top
- **F058** `packages/sidebar-core-rs/src/app.rs:348` — medium · bug · _open_  
  Alt+Up/Down computes the new order from the display list only, so every non-displayed session (collapsed worktree members, filtered-out, hidden) is silently pushed to the end of the persisted order — a collapsed group jumps to the bottom in the default All filter  
  Fix: Make SessionOrder::set_visible_order slot-preserving: walk the existing self.order and replace each name contained in visible_set with the next name from visible_names, leaving non-visible names (collapsed members, filtered, hidden) in their current slots; append any leftover visible names. This turns the collapsed case into [feature-a, feature-b, docs, main] and the filter case into [a, d, c, b]. Alternatively compute `names` in reordered_session_names from the full self.sessions list (minus _o
- **F052** `packages/sidebar-core-rs/src/app.rs:403` — medium · bug · _open_  
  Reordering with a collapsed worktree group: moving past it is a no-op, and moving anything else pushes the collapsed group to the bottom of the persisted order  
  Fix: Build `names` in reordered_session_names from the filtered session list with collapsed members expanded in place (e.g. iterate display entries and, for a collapsed Group entry, splice in worktree_group_session_names(key)), and compute group_indices from that list. This fixes both the no-op and the relocation with one change; alternatively resolve Group-branch indices via worktree_group_session_names and insert collapsed members back into `names` before calling set_visible_order.
- **F049** `packages/sidebar-core-rs/src/renderer.rs:398` — medium · bug · _fixed 2026-09-13_  
  Mouse-wheel scroll can never reveal the last 1-2 sessions: renderer clamps the offset with a stale 2-rows-per-card constant while cards are 3 rows
- **F060** `packages/sidebar-core-rs/src/renderer.rs:419` — medium · bug · _open_  
  Scrollbar is painted over the last content column: per-session agent badge and last diff-stat digit vanish whenever the list overflows (also the closing prompt quote in the agent panel)  
  Fix: Decide up front whether the list overflows (total_rows > available) and, if so, build session rows with `width - 1` (and pass `width - 1` into the agent block builders / prompt_width when agent_scrollbar will be shown), keeping the ScrollbarSpec area at the last column; or shrink the content Rect in render_model and render the scrollbar in its own 1-col Rect.
- **F062** `packages/sidebar-core-rs/src/renderer.rs:1486` — medium · bug · _fixed 2026-09-13_  
  Theme picker draws nothing at the default 26-col width (needs >= 30) or at height < 19, but the invisible modal still captures every key

## Low (71)

- **F089** `README.md:24` — low · bug · _open_  
  README's new 'tmux 3.0 or newer' requirement is wrong: even-horizontal needs join-pane -f (3.1), hook pipelines need list-panes -f (3.2), lazydiff popup needs display-popup (3.2); on 3.0a prefix-o-e strands the sidebar in _os_stash  
  Fix: Change README.md:24 to '`tmux` 3.2 or newer' (and any tutorial requirement list). Optionally have opensessions.tmux compare `tmux -V` against 3.2 and display-message a clear unsupported-version notice instead of installing hooks; at minimum make even-horizontal.sh retry restore without -f (or check `tmux -V`) so a failed restore never leaves the sidebar in _os_stash.
- **F115** `README.md:134` — low · bug · _open_  
  README's documented dev loop (cargo build --release && cargo test --workspace) leaves the tmux E2E suite without target/debug/opensessions-server, so every E2E test panics on a fresh clone  
  Fix: Document the real loop: README.md:130-131 and AGENTS.md:106 -> `cargo build --workspace --bins && cargo test --workspace` (mention OPENSESSIONS_E2E_SERVER_BIN as the override). Alternatively make the test self-sufficient: in server_bin(), when the debug path does not exist, run `$CARGO build -p opensessions-server` (CARGO is set for test processes) before returning it, or fall back to target/release/opensessions-server.
- **F045** `README.md:167` — low · bug · _open_  
  README, CONTRACTS.md and the features reference promise Amp session.json seen-state sync that no runtime has implemented since April 2026  
  Fix: Delete the promise from README.md:167 (keep 'Amp watcher reads ~/.local/share/amp/threads/*.json'), CONTRACTS.md:16, and docs/reference/features-and-keybindings.md:32 and :132. If the feature is wanted instead, add a session.json poll to scan_amp_threads that tracks lastThreadId and calls AgentTracker::mark_seen-style clearing for the matching amp thread instance.
- **F098** `apps/server-rs/src/lib.rs:823` — low · bug · _open_  
  Config `theme` is documented as active/persisted but the server never loads it and `set-theme` never saves it (regression from the TS server)  
  Fix: In `default_state_source_from_env`, seed the theme: `if let Some(name) = config.theme.as_ref().and_then(Value::as_str) { source = source.with_theme(name.to_string()); }` (add the builder); in the `set-theme` handler call `save_config_to_home(&home, OpensessionsConfig { theme: Some(Value::String(theme.clone())), ..Default::default() })` mirroring persist_sidebar_width. Do the same for `sessionFilter`, or downgrade the doc rows to 'parsed only'.
- **F006** `apps/server-rs/src/lib.rs:991` — low · bug · _open_  
  Session metadata is never pruned despite the documented promise: deleted sessions keep their status/progress/logs and a recreated session with the same name inherits them; unknown names accumulate without bound  
  Fix: Call metadata_store.prune_sessions(names) from snapshot_json (or the tmux poll loop) using the full mux session list (sorted_session_names, not the filtered visible list), skipping the prune when the provider returned no sessions because tmux was unreachable; optionally reject metadata for session names not present in the mux with 404.
- **F095** `apps/server-rs/src/lib.rs:1052` — low · bug · _open_  
  Session metadata store is never pruned: status/progress/logs resurface on a re-created session name and the map grows per distinct name posted, contradicting the 'auto-pruned' API doc  
  Fix: In `visible_session_names()` (lib.rs:1690-1701) right after `session_order.sync(names.clone())` add `self.metadata_store.lock().unwrap().prune_sessions(names.iter().cloned());` - prune against ALL live session names (not the visible subset, so hidden sessions keep their metadata). Optionally reject metadata posts for unknown session names.
- **F116** `apps/server-rs/src/lib.rs:1174` — low · unclear · _agent redesign_  
  /api/agent-event trusts client `ts` for TTL pruning: a `ts` in seconds (contract says ms) makes the event reaped in the same request, silently (204), unless a recognised live pane shields it; the `liveness`/`unseen` part of the claim is refuted  
  Fix: Minimal hardening in apply_agent_event (lib.rs:1174-1177): treat implausible ts as invalid — e.g. `ts < 1_000_000_000_000` (before 2001 in ms, i.e. a seconds value) -> either multiply by 1000, fall back to (self.now_ms)(), or return a new AgentEventError::InvalidTimestamp mapped to 400 'ts must be milliseconds'. Optionally reword CONTRACTS.md:111 to say `unseen`/`liveness` are server-derived and ignored on the HTTP path (the field table already implies it).
- **F020** `apps/server-rs/src/lib.rs:1289` — low · bug · _agent redesign_  
  Agent-watcher loop re-resolves every unresolved recent transcript each 2 s tick (4 tmux spawns each), broadcasts one full snapshot per applied snapshot, and never evicts last_seen (uncapped prompts)  
  Fix: In run_agent_watcher_loop: list sessions once per tick (spawn_blocking) and pass the Vec<MuxSessionInfo> into resolve/apply so a resolve costs 0 spawns; keep a second map of unresolved key->fingerprint (plus a hash of the (name,dir) session set) and skip re-resolving until either changes; apply all snapshots first and send a single snapshot_json() after the loop if anything was applied; at the end of each tick `last_seen.retain(|k,_| keys_seen_this_tick.contains(k))` (the scanner already drops e
- **F010** `apps/server-rs/src/lib.rs:1303` — low · bug · _agent redesign_  
  Every unresolved (non-idle, <=5 min old) agent-watcher snapshot triggers two full provider.list_sessions() scans (4 tmux subprocesses with TmuxProvider) on every 2 s tick, inline on the current_thread runtime; the second scan is a regression from the uncommitted resolve_session_for_project_dir refactor  
  Fix: Compute the session list once and reuse it: change resolve_session_for_project_dir to take `&[MuxSessionInfo]` (or the built dir-session map) and pass the `sessions` already collected in resolve_agent_watcher_session; better, build the dir->session map once per watcher tick in run_agent_watcher_loop (before the `for snapshot in snapshots` loop) and pass it into apply_agent_watcher_snapshot so N unresolved snapshots cost one listing per tick instead of 2N.
- **F011** `apps/server-rs/src/lib.rs:1473` — low · bug · _open_  
  git_info_for_dir never caches empty (non-repo / git-missing) results, so every snapshot_json re-spawns `git rev-parse` for each visible session rooted outside a git repository  
  Fix: Delete the early return at lib.rs:1473-1475 (parse_git_info_output already maps an empty string to GitInfo::empty(), git_info.rs:24-28) so the empty result is inserted into git_info_cache with the same 5 s TTL; or explicitly insert CachedGitInfo{info: GitInfo::empty(), ts: now} before returning.
- **F099** `apps/server-rs/src/lib.rs:1604` — low · bug · _open_  
  Config `sidebarPosition` is documented as active but both sidebar spawn sites hard-code SidebarPosition::Left; `mux` is documented as active but never consulted (no user-visible effect today)  
  Fix: Store the loaded position on ReadOnlyMuxStateSource (e.g. `sidebar_position: mux::SidebarPosition`, default Left) via a `with_sidebar_position` builder called from `default_state_source_from_env` (map config::SidebarPosition::{Left,Right} -> mux::SidebarPosition::{Left,Right}) and pass it at lib.rs:1604 and :1654. For `mux`, either pass `config.mux.as_deref()` into a MuxRegistry::resolve call or change the docs row (configuration.md:30, README.md:197) to 'parsed only'.
- **F102** `apps/server-rs/src/lib.rs:1698` — low · unclear · _open_  
  `d` on the attached (current) tmux session is a silent no-op: hide-session is undone by the per-snapshot session_order.show(current), with no client feedback  
  Fix: Pick one and document it: (a) client-side guard — in packages/sidebar-core-rs/src/app.rs:528-533 skip HideSession when the focused session equals my_session/current_session (optionally flash or show a one-line hint), and add 'the session you are attached to cannot be hidden' to docs/reference/features-and-keybindings.md:73; or (b) server-side — in the hide-session arm (lib.rs:798-802) return early when `name == provider.get_current_session()` so the intent is at least explicit, and/or only call 
- **F019** `apps/server-rs/src/lib.rs:1959` — low · bug · _fixed 2026-09-13_  
  Docs still promise an Amp `session.json` seen-state integration that no longer exists (removed in the TS watcher rewrite d7a6287, never ported to Rust)
- **F015** `apps/server-rs/src/lib.rs:2024` — low · bug · _agent redesign_  
  Agent watchers fully re-read and re-parse every recently modified transcript every 2 s even when unchanged (~75 ms CPU + 145 MB transient allocation per tick for the 145 MB transcript on this machine)  
  Fix: Keep a per-path cache keyed by (mtime, len) holding the parsed intermediate state (status, thread_name, last_user_prompt, last_entry_is_tool_use, saw_entry) and skip the read/parse when unchanged, recomputing only the mtime-dependent Waiting/Stale promotion from the cached fields; or, since the files are append-only, remember the last offset and parse only the appended tail (re-parse from scratch if len shrank).
- **F012** `apps/server-rs/src/lib.rs:2596` — low · bug · _open_  
  Server never answers `Expect: 100-continue`, so clients that send it (curl <= 7.68 for >1 KiB bodies, any curl for >= 1 MiB bodies) stall ~1 s per request  
  Fix: In `handle_connection`, after `parse_http_request`, if `parsed.header("expect").is_some_and(|v| v.eq_ignore_ascii_case("100-continue"))` and the body is going to be read, `stream.write_all(b"HTTP/1.1 100 Continue\r\n\r\n").await?` before `read_remaining_http_body` (reply 417 instead when rejecting). Consider also capping Content-Length (e.g. 1 MiB) and returning 413 above it.
- **F007** `apps/server-rs/src/lib.rs:2773` — low · bug · _open_  
  Malformed metadata payloads on /set-status, /set-progress, /log, /notify, /clear-log (wrong field types, missing message, invalid/empty JSON) fall through to the generic 200 OK catch-all instead of 400 - a regression from the pre-Rust server, which returned 400  
  Fix: After the handle_http_json branch (lib.rs:2781-2794) add: `if parsed.method == "POST" && is_metadata_path(&parsed.path) { if serde_json::from_slice::<Value>(http_body(&request)).is_err() { 400 "invalid json" } else { 400 "invalid payload" (or a field-specific message from handle_http_json returning Result) }; shutdown; return }` so the 200 catch-all only serves non-API paths. If state_source is None (no TMUX), answer 503 or 204 rather than 400.
- **F008** `apps/server-rs/src/lib.rs:2848` — low · unclear · _open_  
  The `closing…` snapshot broadcast by begin_shutdown is never delivered to any websocket client on any shutdown path (biased select sends `quit` first; a buffered state frame is discarded when QUIT_JSON arrives; late connections during the drain are reset), while the docs describe closing… -> quit as current behaviour  
  Fix: Either (a) in the websocket loop's shutdown branch, before sending QUIT_JSON, drain state_rx with try_recv() and send the last non-quit state (or the closing snapshot) so clients render `closing…` for the drain window; or (b) remove the begin_shutdown snapshot broadcast from announce_shutdown (keep coordinator.begin_closing() for its gating effect) and update sidebar-behavior.md / chaos-grill.md to say only `quit` is sent. Option (b) is cheaper and matches what users actually see.
- **F009** `apps/server-rs/src/lib.rs:2951` — low · bug · _open_  
  No deadline on HTTP header/body reads: a local client that stalls mid-request holds its connection task, socket and pre-allocated Content-Length buffer for as long as it stays connected  
  Fix: Wrap the read phase: `let mut request = tokio::time::timeout(Duration::from_secs(5), read_http_header(&mut stream)).await.map_err(|_| ServerError::new("http request timed out"))??;` and likewise around read_remaining_http_body; on expiry the task returns and the socket/buffer are dropped. (Also clamp content_length to a sane maximum before resize - covered by the separate allocation finding.)
- **F056** `apps/tui-rs/src/main.rs:462` — low · bug · _open_  
  Theme picker filter cannot receive 'j'/'k': ui_key_from_crossterm aliases them to Down/Up before the modal dispatch, so 'tokyo' becomes query 'toyo'  
  Fix: In main.rs:462-463 stop aliasing Char('j')/Char('k'); emit UiKey::Char for them and do the aliasing in input.rs apply_ui_key's non-modal match (UiKey::Char('j') => same as Down, UiKey::Char('k') => same as Up). If j/k should keep working in the width slider, add Char('j'|'k') arms to apply_width_slider_key (docs only promise h/l/arrows there).
- **F114** `apps/tui-rs/src/main.rs:532` — low · bug · _open_  
  `l` (lazydiff popup) needs tmux >= 3.2 (`display-popup`) but README now promises tmux 3.0 support; on 3.0/3.1 the key silently does nothing  
  Fix: Minimal correct fix: change README.md:24 to '- `tmux` 3.2 or newer' (nothing else in the plugin needs anything newer than 3.0, and `display-popup` with -d/-E/-h/-w is fully supported from 3.2). Optionally harden `launch_lazydiff` (apps/tui-rs/src/main.rs:530-542): keep the `Output`, and if `!status.success()` fall back to `tmux split-window -c <dir> -l 90% <cmd>` (or `new-window -c <dir> <cmd>`), and log the stderr via `debug_log` so failures are not silently swallowed.
- **F072** `apps/tui-rs/src/main.rs:542` — low · bug · _open_  
  'l' runs `tmux display-popup -E` with Command::output() on the current_thread runtime, freezing the whole TUI event loop until the lazydiff popup closes  
  Fix: Use .spawn() instead of .output() for the display-popup command and reap the child off the loop (e.g. std::thread::spawn(move || { let _ = child.wait(); }) or enable tokio's `process` feature and use tokio::process::Command, which reaps dropped children). The 750 ms debounce in maybe_launch_lazydiff still prevents double launches.
- **F076** `apps/tui-rs/src/main.rs:567` — low · bug · _open_  
  'L' spawns x-terminal-emulator/xterm with inherited stdio and drops the Child, leaving one zombie per launch for the sidebar's lifetime  
  Fix: Configure .stdin(Stdio::null()).stdout(Stdio::null()).stderr(Stdio::null()) (or stderr to the err log), detach with process_group(0)/setsid so pane hangups don't kill the terminal, and reap the child (background thread calling wait(), or tokio::process with the process feature).
- **F077** `apps/tui-rs/src/main.rs:706` — low · unclear · _open_  
  No panic hook restores the terminal; with panic = "abort" in the release profile a (currently unknown) panic would leave raw mode/alt screen/mouse capture on when the release sidebar is run outside tmux  
  Fix: Optional hardening: before TerminalGuard::enter(), install std::panic::set_hook that runs disable_raw_mode + execute!(stdout, Show, DisableMouseCapture, LeaveAlternateScreen) and then calls the previous hook; or build the sidebar binary with panic = "unwind".
- **F074** `apps/tui-rs/src/runtime_config.rs:6` — low · bug · _open_  
  Sidebar (bytes), runtime (chars) and launcher (awk printable-ASCII table) hash the tmux socket path differently, so with a non-ASCII socket path every plugin-spawned sidebar dials the wrong port and dies  
  Fix: Have the server hand its own endpoint to the pane so the sidebar never re-derives it: store the host/port given to TmuxProvider::setup_hooks (tmux_provider.rs:530, same pattern as set_sidebar_width_hint) and add ("OPENSESSIONS_HOST", host), ("OPENSESSIONS_PORT", port) to the env slice in spawn_sidebar (tmux_provider.rs:844-848); cli.rs already honors OPENSESSIONS_PORT. Independently, collapse the three hash definitions to one: make apps/tui-rs/src/runtime_config.rs re-export opensessions_runtime
- **F075** `apps/tui-rs/src/runtime_config.rs:21` — low · bug · _open_  
  Runtime server still defaults to port base 17000 behind the removed OPENSESSIONS_RUST flag while sidebar and launcher use 22000, so a server started by hand inside tmux is unreachable and the plugin starts a second one  
  Fix: Finish what commit 89168a3 started: in packages/runtime-rs/src/shared.rs make 22_000 the only base (resolve_server_port -> resolve_server_port_with_base(.., 22_000), drop the rust_port_base parameter from OpensessionsEndpoint::from_env, and stop reading OPENSESSIONS_RUST in resolve_server_settings), then delete the now-redundant 'OPENSESSIONS_RUST=1 \' line at server-common.sh:134. Ideally expose one shared constant/function that apps/tui-rs/src/runtime_config.rs re-exports so the sidebar and se
- **F078** `apps/tui/scripts/start.sh:5` — low · bug · _open_  
  start.sh (and server-common.sh) truncate OPENSESSIONS_DIR at the first `=` in its value when reading it back from tmux, so a plugin checkout under a path containing `=` cannot find its binaries  
  Fix: apps/tui/scripts/start.sh:5 -> `OPENSESSIONS_DIR="$(tmux show-environment -g OPENSESSIONS_DIR 2>/dev/null | cut -d= -f2-)"` (or read the line and use `${line#*=}`), and only consult tmux when the inherited `$OPENSESSIONS_DIR` is empty (e.g. `if [ -z "${OPENSESSIONS_DIR:-}" ] && [ -n "${TMUX:-}" ]; then ...`) so a correct value passed by tmux's merged environment is never overwritten; optionally treat a value starting with `-` (tmux's removed-variable marker) as empty. Make the same `-f2-` change
- **F073** `apps/tui/scripts/start.sh:28` — low · bug · _open_  
  start.sh aborts the sidebar launch whenever the fixed shared path /tmp/opensessions-err.log is not writable (second user on a host, or any file/symlink planted there by another local user)  
  Fix: In apps/tui/scripts/start.sh replace the two hard-coded redirections with a per-user path opened defensively, e.g. `ERR_LOG="${TMPDIR:-/tmp}/opensessions-$(id -u)-err.log"` (or `${XDG_STATE_HOME:-$HOME/.local/state}/opensessions/sidebar-err.log`, which also removes the /tmp symlink/squat vector), then `if [ -L "$ERR_LOG" ] || ! { printf '\n--- launch %s pid=%s ---\n' "$(date +%s)" "$$" >>"$ERR_LOG"; } 2>/dev/null; then ERR_LOG=/dev/null; fi` and `exec "$RUST_BIN" 2>>"$ERR_LOG"` - a log that cann
- **F113** `integrations/pi-extension/opensessions-runtime.ts:98` — low · bug · _open_  
  Pi extension stops at a non-owning server's 202 (and at any server's unconditional 204 for /api/runtime/pi/*), so with an ambient OPENSESSIONS_URL/PORT/SERVER_KEY override the owning server never receives its live Pi events  
  Fix: In integrations/pi-extension/opensessions-runtime.ts post(): for /api/agent-event treat only `response.status === 204` as delivered and continue to the next candidate on 202 (mirror amp postOnce, lines 274-279); for /api/runtime/pi/upsert and /api/runtime/pi/delete post to every candidate URL instead of stopping at the first 2xx (both are harmless on non-owners: upsert only adds a registry entry, delete on a non-owner resolves no session and returns Ok(None)). Also add the Amp plugin's /tmp/open
- **F117** `integrations/tmux-plugin/scripts/server-common.sh:9` — low · bug · _open_  
  uninstall.sh run outside tmux derives port 7391 and /tmp/opensessions.pid, so /quit misses the real server, which keeps running with its pid file  
  Fix: In server_key(), when $TMUX is unset fall back to `tmux display-message -p '#{socket_path}'` (works from any shell while the default tmux server runs) before giving up; and in uninstall.sh, after the /quit attempt, `kill "$(cat "$PID_FILE")"` when the pid file exists, or at least document that the script must be run from inside the tmux server being uninstalled.
- **F088** `integrations/tmux-plugin/scripts/server-common.sh:16` — low · bug · _open_  
  Shell awk, runtime-rs (code points) and tui-rs (bytes) server-key hashes disagree for non-ASCII/control characters in the tmux socket path, so the sidebar connects to the wrong port  
  Fix: Stop re-deriving in the sidebar: pass the server's own endpoint to the pane with split-window -e (add ("OPENSESSIONS_PORT", port) and ("OPENSESSIONS_HOST", host) to the env array in TmuxProvider::spawn_sidebar; the provider already receives host/port in setup_hooks). Secondarily make shared.rs hash_server_key iterate bytes so the two Rust hashes agree, and treat the awk value as the canonical one only for ASCII (or move the key derivation to a tiny Rust helper).
- **F093** `integrations/tmux-plugin/scripts/server-common.sh:38` — low · bug · _open_  
  Non-numeric OPENSESSIONS_SERVER_KEY kills every helper script with 'Illegal number' under dash and desynchronises script/sidebar ports under bash, while the Rust side silently falls back  
  Fix: In server_key(): `case "$OPENSESSIONS_SERVER_KEY" in ''|*[!0-9]*) ;; *) printf '%s\n' "$OPENSESSIONS_SERVER_KEY"; return;; esac` so a non-numeric key falls through to the socket hash exactly like the Rust code (optionally show_startup_error about the ignored value), and state in configuration.md that the key must be a small integer (port = 22000 + key).
- **F091** `integrations/tmux-plugin/scripts/uninstall.sh:19` — low · bug · _open_  
  uninstall.sh run outside tmux derives port 7391 and /tmp/opensessions.pid, so /quit misses the real server (22000+key) while the tmux sweep still strips hooks/panes/env from the default socket  
  Fix: In server-common.sh's server_key(), when $TMUX is unset fall back to `tmux display-message -p '#{socket_path}'` (empty if no server) before giving up, so PORT/PID_FILE match the server that the bare tmux commands are already targeting; additionally in uninstall.sh check `server_alive` before the /quit and after the sleep and print a warning (with the pid file path / `kill $(cat $PID_FILE)` hint) instead of the unconditional '✓ stopped server (if running)' when the server was reachable but did no
- **F092** `opensessions.tmux:107` — low · unclear · _open_  
  Direct prefix bindings C-s/C-t/M-1..9 are unconditional, shadow tmux's built-in M-1..M-5 layout keys and tmux-resurrect's prefix C-s, are absent from the configuration reference's binding table, and uninstall leaves the shadowed tmux defaults unbound until tmux restarts  
  Fix: If the maintainer wants to keep the defaults: list `prefix C-s`, `prefix C-t`, `prefix M-1..M-9` in the configuration.md binding table with a note that they shadow tmux's M-1..M-5 layout keys, and add an option (e.g. `@opensessions-direct-bindings on|off`) so users of tmux-resurrect or the layout keys can turn them off. In uninstall.sh, after unbinding, restore the tmux defaults for the keys it shadowed (`bind-key -T prefix o select-pane -t :.+`, `M-1 select-layout even-horizontal`, `M-2 even-ve
- **F046** `packages/runtime-rs/src/agent_watchers.rs:297` — low · bug · _agent redesign_  
  Droid parser lets a per-line 'id' on any entry override the session id, so transcripts whose message lines carry ids get a new thread_id (and tracker instance) per message  
  Fix: Only accept `id` as the session id from a header line (`type == "session_start"` / `"session"`, or a line without `message`/`role`/`hook_event_name`), keep `session_id`/`sessionId` from hook lines, and never let a later line override an already-found session id (`if session_id_from_file.is_none()`). Also verify against a real Droid install whether transcripts live in ~/.factory/sessions rather than ~/.factory/projects and scan both if needed.
- **F040** `packages/runtime-rs/src/agent_watchers.rs:467` — low · bug · _agent redesign_  
  Claude Code parser treats isMeta and isCompactSummary user entries as real prompts: /context output or skill bodies become thread names / last prompts and compaction summaries become the last prompt  
  Fix: In extract_claude_user_prompt return None when `entry.get("isMeta") == Some(&Value::Bool(true))` or `entry.get("isCompactSummary") == Some(&Value::Bool(true))` (and skip them in determine_claude_code_status for consistency). Optionally prefer Claude Code's explicit entries: `type == "last-prompt"` -> `lastPrompt` for last_user_prompt, and `type == "ai-title"` -> `aiTitle` as a title source below `custom-title`.
- **F097** `packages/runtime-rs/src/config.rs:52` — low · bug · _open_  
  load_config_from_home silently discards the whole config when any field has an unexpected type, so a persisted sidebarWidth is never restored and nothing is logged  
  Fix: Minimal: at config.rs:52-54 keep the serde error and report it (eprintln! so it lands in /tmp/opensessions.<key>.server.log, and/or debug_log::log_with_tag) naming the path and the field. Better: parse to serde_json::Value first, then deserialize each field individually via serde_json::from_value with a logged per-field fallback to None, so one bad field cannot discard sidebarWidth/detailPanelHeight; add a unit test for the wrong-typed-sibling case.
- **F096** `packages/runtime-rs/src/config.rs:65` — low · bug · _open_  
  save_config_to_home silently replaces an unparseable config.json with {plugins:[]}+update on the next slider/height save, dropping every hand-written key (non-atomic write is real but has no realistic trigger)  
  Fix: In save_config_to_home distinguish 'file absent' (start from {plugins:[]}) from 'file present but unreadable/unparseable': for the latter return Err(io::Error::other("config.json is not valid JSON; refusing to overwrite")) - both callers already debug_log the error - or rename the broken file to config.json.bak before writing. Independently, write via `config.json.tmp` + fs::rename to make the write atomic (session_order.rs:150 has the same fs::write pattern and could share the helper). Add a re
- **F018** `packages/runtime-rs/src/git_info.rs:31` — low · bug · _open_  
  parse_git_info_output splits on the first bare '---': a branch name or repo path containing '---' truncates the branch, reports phantom dirty state, and (for linked worktrees) clears is_worktree  
  Fix: Minimal change confined to git_info.rs:30-37: anchor both separators to whole lines instead of bare substrings, e.g. `output.split_once("\n---\n")` for the header split, and for the second split accept the token at the start of the (trimmed) rest or at a line start: `if let Some(n) = rest.strip_prefix("---NUMSTAT---") { ("", n.trim()) } else { rest.split_once("\n---NUMSTAT---").map(|(s, n)| (s.trim(), n.trim())).unwrap_or((rest, "")) }`. Branch names cannot contain newlines and porcelain status 
- **F112** `packages/runtime-rs/src/metadata_store.rs:93` — low · bug · _open_  
  Metadata store is never pruned: deleted-session metadata resurfaces on a re-created same-named session and unknown session names accumulate forever, contrary to the documented auto-prune  
  Fix: Wire the existing pruner into the snapshot path using the FULL tmux listing (not `visible_session_names`, whose `SessionOrder::apply` drops hidden sessions, which would delete metadata of merely-hidden sessions). Minimal patch in apps/server-rs/src/lib.rs `visible_session_names` (line 1689), right after `let names = self.sorted_session_names();`: `self.metadata_store.lock().unwrap().prune_sessions(names.iter().cloned());`. This mirrors how `session_order.sync(names.clone())` already reconciles p
- **F047** `packages/runtime-rs/src/pi_runtime_registry.rs:36` — low · bug · _agent redesign_  
  Pi runtime registry TTL is never enforced: the server only calls upsert/delete, so entries from pi processes that exit without a delete accumulate (~300 B each) for the server's lifetime  
  Fix: Wire the existing prune into the periodic path: in prune_agents (apps/server-rs/src/lib.rs:555) add `self.pi_runtime_registry.lock().unwrap().prune(now);` (optionally fold its bool into `changed` is unnecessary since the registry is not part of the snapshot). snapshot_json already calls prune_agents on every poll tick, so heartbeating pi processes (5 s cadence) stay registered while dead ones expire after 20 s, matching the heartbeat/TTL design; delete remains a plain remove so a genuine session
- **F118** `packages/runtime-rs/src/project_dir_session.rs:58` — low · unclear · _agent redesign_  
  Two sessions rooted at the same directory make every projectDir-only agent (all transcript watchers) unresolvable; ambiguity rule is deliberate but undocumented  
  Fix: At minimum document the rule in CONTRACTS.md (Session Resolution): 'if more than one session matches at the winning stage, the event is not attributed (watcher snapshots are dropped; HTTP events fall back to tmuxSession or get 202)'. If the maintainer prefers attribution over hiding: in ReadOnlyMuxStateSource::resolve_session_for_project_dir, when the pure resolver returns None but several sessions share the winning dir, tiebreak by pane presence - pick the unique candidate session for which pro
- **F106** `packages/runtime-rs/src/shared.rs:57` — low · bug · _open_  
  Duplicate of F101: server port base is 17000 unless OPENSESSIONS_RUST=1 while sidebar, helper scripts and TS integrations use 22000, so a directly-launched server inside tmux is unreachable and gets duplicated by the plugin  
  Fix: Same as F101: make 22000 the only base in shared.rs (remove the OPENSESSIONS_RUST switch and the 17000 constant, fix the comment), and drop `OPENSESSIONS_RUST=1` from server-common.sh:134. Treat as one fix with F101, not two.
- **F100** `packages/runtime-rs/src/shared.rs:77` — low · bug · _open_  
  Server-key hash differs between the awk launcher (printable-ASCII table), the sidebar (bytes), the runtime (chars) and the TS integrations (UTF-16) for non-ASCII tmux socket paths, so the sidebar dials a port the server never binds  
  Fix: Stop making the sidebar re-derive the port: have the tmux provider remember the host/port it received in `setup_hooks` (or thread them into `spawn_sidebar`) and pass `OPENSESSIONS_HOST=...`/`OPENSESSIONS_PORT=...` through the existing `split-window -e` list in `split_sidebar_pane`, so the sidebar always dials the port the server actually bound. Independently, make every hash iterate raw bytes (Rust: `input.bytes()` in shared.rs; awk: feed byte values, e.g. `printf '%s' "$socket_path" | LC_ALL=C 
- **F101** `packages/runtime-rs/src/shared.rs:153` — low · bug · _open_  
  opensessions-server defaults to the removed legacy port base 17000 unless OPENSESSIONS_RUST=1, so a server started directly inside tmux (the documented `cargo run -p opensessions-server`) binds a port the sidebar and plugin scripts never dial  
  Fix: In `resolve_server_settings` drop the OPENSESSIONS_RUST lookup and call `OpensessionsEndpoint::from_env(env, true)` (or replace the `rust_port_base` flag with a single `SERVER_PORT_BASE: u32 = 22_000` used by both `from_env` and `resolve_server_port`), update the shared.rs:106-110 comment, and remove the now-redundant `OPENSESSIONS_RUST=1` from server-common.sh:134. Add a unit test asserting that TMUX=/private/tmp/tmux-501/default,13614,3 resolves to 41916 like apps/tui-rs/tests/cli.rs does for 
- **F104** `packages/runtime-rs/src/sidebar_coordinator.rs:91` — low · bug · _open_  
  identify-pane from an already-killed sidebar pane re-marks the coordinator visible after /toggle hide, so the next ensure-sidebar hook respawns a sidebar the user just closed (rare race: TUI must be starved between hello and identify)  
  Fix: In `handle_sender_command_with_context` (apps/server-rs/src/lib.rs:968-971) only acknowledge when the identifying pane is still a live sidebar pane, e.g. `let alive = context.pane_id.as_deref().is_some_and(|id| self.providers.iter().any(|p| p.list_sidebar_panes(None).iter().any(|pane| pane.pane_id == id)));` and call `acknowledge_sidebar_connected()` (and the width repair) only if `alive`; still return the `your-session` reply. Do not make `acknowledge_sidebar_connected` a no-op for Idle: manual
- **F032** `packages/runtime-rs/src/tmux_provider.rs:180` — low · bug · _open_  
  Sidebar-initiated switch-session never passes a client, so tmux switches its most-recently-active client, which can be another attached terminal/person  
  Fix: In handle_client_command_with_context for 'switch-session' (and switch-index / the kill-session fallback), when no clientTty is supplied use context.session_name to pick the client(s): `list-clients -t =<session> -F #{client_tty}` and issue `switch-client -c <tty> -t <target>` for each (or the most recently active), mirroring the fallback logic already used in kill_orphaned_sidebar_panes_with_fallbacks (tmux_provider.rs:812-817). Alternatively expose a provider method switch_session_for_session_
- **F030** `packages/runtime-rs/src/tmux_provider.rs:236` — low · bug · _open_  
  flash_pane's `select-pane -P default` leaves explicit pane-level window-style/window-active-style that permanently shadow the user's global styles on every sidebar-focused agent pane  
  Fix: In flash_pane, restore by unsetting the pane-level options instead of writing 'default': `sleep 0.18; tmux set-option -p -t '%N' -u window-style; tmux set-option -p -t '%N' -u window-active-style` (tmux >= 3.1). If pre-existing pane-level styles must survive, read them first with `show-options -pv -t %N window-style`/`window-active-style` and write them back; otherwise unset is sufficient.
- **F029** `packages/runtime-rs/src/tmux_provider.rs:739` — low · bug · _open_  
  resolve_agent_pane_id's amp thread lookup and thread_name_from_pane assume contradictory Amp title formats; for the '<thread> - amp - ...' format the rest of the code/tests use, the thread-specific match is dead code and focus/kill fall through to the first amp pane  
  Fix: Make the amp match consistent with thread_name_from_pane and tolerant of both formats, e.g. in resolve_agent_pane_id replace the filter with: `thread_name_from_pane(pane, "amp").as_deref() == Some(thread_name) || (pane.title.to_lowercase().starts_with("amp - ") && pane.title.contains(thread_name))`, and add a unit test using the '<thread> - amp - <x>' titles the E2E suite uses. Confirm with the current Amp CLI which title format it emits and drop the obsolete branch.
- **F033** `packages/runtime-rs/src/tmux_provider.rs:1025` — low · bug · _open_  
  Raw tab/newline in #{pane_current_path} (and #{session_path}) corrupts tab-separated pane/session parsing; a newline in a content pane's cwd can get the window's sidebar killed as an orphan  
  Fix: Simplest: drop #{pane_current_path} from pane_format (PaneInfo.cwd is unused) or move it to the last field and parse with splitn; otherwise sanitise in the format itself with tmux's s/ modifier using literal characters, e.g. `#{s/<TAB>/ /:#{s/<LF>/ /:pane_current_path}}` (note `#{q:}` does not help). Apply the same to #{session_path} in session_format (or sanitise) and add a field-count guard in parse_sessions.
- **F026** `packages/runtime-rs/src/tmux_scripting.rs:221` — low · bug · _open_  
  Hook-side orphan/dead-pane fallback picks the alphabetically previous session with no exclusion of the internal `_os_stash` session, so closing the last pane of the first lowercase-named session parks the client in the hidden stash session  
  Fix: In both pipelines use `list-sessions -f '#{!=:#{session_name},_os_stash}'` (or add `$0=="_os_stash"{next}` to the awk) so the stash is never a fallback; longer term either kill the stash after even-horizontal restores the pane / on shutdown (call cleanup_sidebar), or publish the server's display-order neighbour map into a tmux user option the hook can read so hook and server agree.
- **F034** `packages/runtime-rs/src/tmux_scripting.rs:286` — low · bug · _open_  
  Unquoted #{socket_path} in run-shell hook pipelines breaks the synchronous orphan sweep / width repair when the tmux socket path contains whitespace  
  Fix: In packages/runtime-rs/src/tmux_scripting.rs replace every `tmux -S #{{socket_path}}` with `tmux -S '#{{socket_path}}'` in close_orphan_sidebar_pipeline (line 221), close_dead_content_pane_pipeline (line 268) and resize_sidebar_width_pipeline (line 286), and update the expected strings in the tests at lines 393, 402 and 425. Single quotes are the right choice because tmux_double_quote (line 208) escapes only backslash, double-quote and dollar, and the raw `run-shell -b "{}"` built at tmux_provid
- **F031** `packages/runtime-rs/src/tmux_scripting.rs:292` — low · bug · _open_  
  Width repair (hook pipelines and server enforce/repair paths) resize-panes the hidden sidebar while a content pane is zoomed, which unzooms the window after any client/window resize  
  Fix: Add `#{==:#{window_zoomed_flag},0}` to sidebar_width_repair_filter, include window_zoomed_flag in pane_format()/SidebarPane so sidebar_panes_to_resize and repair_context_sidebar_width skip zoomed windows, and treat repair-width from a sidebar whose window is zoomed as a no-op. The width is then repaired on unzoom: `resize-pane -Z` fires after-resize-pane (already installed) and the sidebar TUI sends repair-width when its pane becomes visible again.
- **F042** `packages/runtime-rs/src/tracker.rs:103` — low · bug · _agent redesign_  
  mark_seen clears the unseen set but not the stored event.unseen flag, so after the websocket mark-seen command the session reports unseen:false while every agent row still serializes unseen:true  
  Fix: Derive the serialized flag purely from the set in get_agents/get_state: `event.unseen = self.unseen_instances.contains(&key).then_some(true)` (overwriting in both directions), or additionally set event.unseen = None for each instance inside mark_seen.
- **F043** `packages/runtime-rs/src/tracker.rs:695` — low · bug · _agent redesign_  
  Tracker bookkeeping for killed/renamed sessions is never released: event_timestamps, empty instance maps and Alive entries of sessions no longer in list_sessions live for the life of the server  
  Fix: In sync_agent_pane_presence (lib.rs:533-537) pass the live session-name set to a new tracker method (e.g. retain_sessions) that, for every tracked session not in the set, either drops it outright or applies apply_pane_presence(session, vec![]) so its Alive entries flip to Exited/exited_at and are reaped by the existing prune rules; and have remove_instance/prune_where/dismiss drop event_timestamps[session] (and the empty map) when a session's last instance goes.
- **F044** `packages/runtime-rs/src/tracker.rs:863` — low · unclear · _fixed 2026-09-13_  
  Stale counts as a terminal state for unseen tracking (a silent tool call >15 s marks the session unseen) while CONTRACTS.md/README/features docs say unseen is only for done/error/interrupted
- **F105** `packages/runtime-rs/src/watch_plan.rs:32` — low · unclear · _open_  
  watch_plan and lifecycle_operation are orphaned migration-plan models; portless::load_portless_state was never wired into the Rust server, so the TS server's portless local-link behaviour was silently dropped  
  Fix: Two independent decisions for the maintainer. (a) Delete `packages/runtime-rs/src/watch_plan.rs` and `packages/runtime-rs/src/lifecycle_operation.rs` plus their `pub mod` lines at `packages/runtime-rs/src/lib.rs:6` and `:22`; they model an abandoned notify-based watch service and an abandoned reducer whose tests were removed in 6310a58, and their numbers/roots no longer match the shipped scanners. Update `docs/explanation/sidebar-state-chaos-grill.md:130-131` to stop citing deleted files (or mar
- **F059** `packages/sidebar-core-rs/src/app.rs:227` — low · bug · _open_  
  tmux rename-session permanently strands the sidebar's local-session identity: active marker disappears, cursor becomes invisible, Tab/l act on the wrong session until the sidebar is respawned  
  Fix: Server side (smallest): the tmux poll already lists panes with pane_id and session_name; when a connected client's pane_id now maps to a session_name different from the one it identified with, send it `your-session` with the new name (App's YourSession handler already calls confirm_local_session(name, true), which restores marker/focus). Also make the client re-resolve `tmux display-message -p -t $TMUX_PANE '#{session_name}'` on ReIdentify instead of resending the stored identity, or key identit
- **F051** `packages/sidebar-core-rs/src/app.rs:379` — low · bug · _open_  
  Alt+Up on a worktree-group member in a 3+ member group jumps it to the top of the group  
  Fix: In the `*indented && delta < 0` branch, first look up the moving session and compare keys: if `worktree_group_key(moving) == Some(key)` fall through to the swap (`names.swap(current, target)`); only jump to `group_indices.min()` when the mover is not a member of that group.
- **F055** `packages/sidebar-core-rs/src/app.rs:587` — low · bug · _open_  
  When the local session is not in the display list (filtered out, hidden with d, or renamed), Down skips the first visible row, Up/Shift+Tab are dead, and Tab switches to the second visible session  
  Fix: In move_focus and switch_to_relative_session keep the anchor lookup as Option<usize>; when it is None treat the anchor as 'before the list': next_idx = 0 for delta > 0 and (max_idx or 0) for delta < 0, instead of unwrap_or(0). Alternatively make rehome_focus_to_local_session fall back to the first display entry when the local session is not visible, and keep the local-session anchor only for the active marker.
- **F054** `packages/sidebar-core-rs/src/app.rs:649` — low · bug · _open_  
  All-scope agents panel is not rendered while a worktree group header is focused, yet Ctrl+J/Enter/x/d still operate on the invisible agent list (x kills a pane with no confirmation)  
  Fix: Either make focused_agents_len()/focused_agent() return 0/None when focused_session_name() is None (matching the Right-arrow guard and render_detail), or make render_detail in All scope skip only the path/links/metadata block and still render render_agent_panel_header + all_agent_entries when no session is focused. The first is the smaller change; the second keeps 'all' meaning all.
- **F057** `packages/sidebar-core-rs/src/app.rs:726` — low · bug · _open_  
  Docs (README.md:172, features-and-keybindings.md:26 and :96) promise clicking a port opens http://localhost:<port>, but the Rust TUI has no port/link hit target or URL launcher  
  Fix: Either implement it (push_hit on the local-link rows and port badge with a new HitTarget::Link(url), map it in activate_hit_target to a new LaunchTarget::OpenUrl{url} that main.rs runs via xdg-open/open with a spawn, no shell), or delete the sentence from README.md:172 and features-and-keybindings.md:26 and :96.
- **F053** `packages/sidebar-core-rs/src/app.rs:866` — low · intended · _fixed 2026-09-13_  
  Docs say width-slider Esc reverts, but Esc is deliberately 'close and keep the live value' (doc error)
- **F069** `packages/sidebar-core-rs/src/input.rs:255` — low · bug · _open_  
  Mouse input is not gated by an open modal: clicks, wheel, hover and separator drags act on the rows around/under the dialog while it stays open  
  Fix: At the top of `apply_ui_mouse` (packages/sidebar-core-rs/src/input.rs:214) add: `if app.is_modal_open() { if matches!(event, UiMouse::DragEnd) { app.resize_drag_state = None; } return; }` (keep DragEnd so a drag that was in progress when the modal opened is released). Optionally, for parity with the 'any other key cancels' semantics, treat a Click outside the box as a cancel for KillConfirm (`app.modal = Modal::None`) and as Esc for the theme picker/width slider (`close_theme_picker` / `close_wi
- **F111** `packages/sidebar-core-rs/src/renderer.rs:26` — low · bug · _open_  
  Documented 'click a port to open http://localhost:<port>' has no implementation: the ⌁port badge click switches session and the detail-panel 'local localhost:<port>' row click does nothing  
  Fix: Either implement or un-document. Minimal implementation: add `HitTarget::OpenUrl(String)`; in `build_session_detail_row` (renderer.rs:1058-1064) push the `⌁port` badge with `line.push_hit(port_text, port_color, HitTarget::OpenUrl(format!("http://localhost:{}", session.ports[0])))` (or better, use `session.local_links[0].url` so portless routes are honored), and in `render_detail` (renderer.rs:1160-1176) push each link label with `push_hit(..., HitTarget::OpenUrl(link.url.clone()))`; add `LaunchT
- **F067** `packages/sidebar-core-rs/src/renderer.rs:308` — low · unclear · _open_  
  Header spinner count uses the attention-signal set (incl. error/stale/interrupted) and counts ALL sessions rather than visible ones, so it disagrees with group '⠹N' counts and the 'running' filter  
  Fix: Pick one definition: either count with the shared running predicate (Running|ToolRunning|Waiting, e.g. reuse session_display's session_has_active_agent) over app.filtered_sessions(), or keep the attention set but render it with a non-spinner glyph (e.g. ⚡ as documented) and apply the same set in group_summary. In both cases iterate filtered_sessions() so the count matches the visible list as the design doc states.
- **F064** `packages/sidebar-core-rs/src/renderer.rs:739` — low · bug · _open_  
  Keyboard-focus viewport always ends on the focused NAME row, so the focused card's branch/ports/diff row is hidden whenever row parity lines up (session rows ≡ 1 mod 3 with no groups; the failing residue shifts when a worktree group header is present)  
  Fix: In compute_session_window_start_for_focus anchor the walk-back on the last row of the focused entry: `let anchor = if available_rows >= 2 && matches!(rows.get(focused_idx + 1), Some(SessionListRow::SessionDetail { .. })) { focused_idx + 1 } else { focused_idx };` and start the `start`/`used` walk from `anchor` (keep `used = 1`). The forward skip over Detail/Spacer rows then still lands on a Name/Group row <= focused_idx, and the window [start, start+available) always contains both the name and d
- **F063** `packages/sidebar-core-rs/src/renderer.rs:1064` — low · bug · _open_  
  Documented 'click a port to open it in the browser' is not implemented: port text and local links carry no hit target (clicking a port switches sessions instead)  
  Fix: Either add `HitTarget::Link(String)` pushed via push_hit on the port text (line 1064, url http://localhost:<port>) and on each local link label (line 1176), handle it in App::activate_hit_target by emitting a new ClientCommand/TUI-side action that spawns `xdg-open`/`open` (as the migration plan specified), or delete the two sentences at features-and-keybindings.md:26 and :96.
- **F068** `packages/sidebar-core-rs/src/renderer.rs:1157` — low · bug · _open_  
  Detail panel uses fixed truncation budgets (path 24, thread name 30/26/18, links none) independent of sidebar width, so narrow panes show a misleading '…' path with the basename clipped and thread names cut without an ellipsis even at the default width  
  Fix: Budget by the actual width like build_session_name_row does: `truncate_left(&session.dir, width.saturating_sub(1))`, `truncate_right(thread_name, width.saturating_sub(4))` (and width-based budgets in compact_agent_panel_block and for link labels); if the old constants are meant as caps in wide panes, use `min(24, width-1)` / `min(30, width-4)`.
- **F066** `packages/sidebar-core-rs/src/renderer.rs:1447` — low · bug · _open_  
  Metadata status/log text and thread names are pushed unsanitized: control characters vanish in ratatui (newline/tab glue words, ESC leaves visible '[31m'), and StyledLine::width over-counts them, leaving a highlight gap  
  Fix: Sanitize at ingestion in metadata_store (set_status/append_log/set_progress label) and in the HTTP agent-event threadName/lastUserPrompt path: replace `char::is_control` chars with ' ' (or split status/log on '\n' into extra rows) before storing; optionally also make StyledLine::push map control chars to ' ' so measured and painted widths always agree.
- **F061** `packages/sidebar-core-rs/src/renderer.rs:2012` — low · bug · _open_  
  Footer hint wrapping splits key glyphs from their labels and drops labels; at the default 26-col width the second line reads 'f filter  w width  dx kill'  
  Fix: Group hints as (key, label) pairs and measure/emit each pair atomically: if `key.width()+label.width()` doesn't fit on the current line, move the whole pair to the wrapped line; if it doesn't fit there either, drop the whole pair (and stop). Optionally use a shorter hint set below ~32 cols.
- **F065** `packages/sidebar-core-rs/src/renderer.rs:2134` — low · bug · _open_  
  wrap_truncated_prompt measures intermediate wrap lines per-char, so VS16 emoji sequences produce over-wide lines that Paragraph clips silently (ZWJ sequences wrap too early)  
  Fix: In the cut loop iterate grapheme clusters (unicode-segmentation, already a transitive dependency) and add `grapheme.width()` (UnicodeWidthStr), tracking byte offsets; or keep the char loop but re-check `rest[..cut].width() <= max_cols` with the str implementation before committing the cut (backing off to the previous boundary otherwise).
